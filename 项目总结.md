# Offer 题库助手项目总结

更新时间：2026-04-29

## 1. 项目定位

这是一个本地单用户的个人求职题库系统，用来管理面试题、答案、笔记和标签。当前第一阶段已经从基础 CRUD 升级成一个偏“复习效率工作台”的前后端分离项目。

当前项目名称：

```text
Offer 题库助手 / Interview Question Bank
```

## 2. 当前技术栈

后端：

```text
FastAPI
SQLAlchemy
Pydantic
SQLite
uv
```

前端：

```text
React
TypeScript
Vite
Axios
React Router
Lucide React
Tailwind CSS v4
```

数据库：

```text
backend/offer_bank.db
```

## 3. 当前目录结构重点

```text
backend/
  app/
    main.py              FastAPI 入口、CORS、路由挂载
    database.py          SQLite 连接和数据库 Session
    models.py            SQLAlchemy 表模型
    schemas.py           Pydantic 请求和响应模型
    crud.py              数据库操作逻辑
    routers/
      questions.py       题目接口
      tags.py            标签接口
      export.py          Markdown 导出接口
  pyproject.toml         uv 项目依赖
  requirements.txt       后端依赖冻结文件

frontend/
  src/
    App.tsx              前端路由
    index.css            全局样式和工作台 UI
    api/                 Axios 请求封装
    components/          复用组件
    pages/               页面组件
    types/               TypeScript 类型
    utils/
      questionStatus.ts  根据标签推导掌握状态
```

## 4. 后端已完成功能

### 标签 API

```text
GET    /api/tags
POST   /api/tags
DELETE /api/tags/{id}
```

已支持：

```text
创建标签
查询标签
删除标签
重复标签名校验
删除标签后不删除题目
```

### 题目 API

```text
GET    /api/questions
GET    /api/questions/{id}
POST   /api/questions
PUT    /api/questions/{id}
DELETE /api/questions/{id}
```

已支持：

```text
新增题目
查看题目列表
查看题目详情
编辑题目
删除题目
题目绑定多个标签
```

### 多标签筛选

接口形式：

```text
GET /api/questions?tag_ids=1,2,3
```

筛选规则：

```text
只返回同时包含所选全部标签的题目
```

### Markdown 导出

接口：

```text
GET /api/export/questions.md
GET /api/export/questions.md?tag_ids=1,2
```

已支持：

```text
导出全部题目
按标签筛选后导出
下载文件名：offer-question-bank.md
```

## 5. 前端已完成功能

### 工作台布局

当前左侧导航包括：

```text
复习面板
题目库
新增题目
标签配置
导出中心
```

布局文件：

```text
frontend/src/components/AppLayout.tsx
```

路由文件：

```text
frontend/src/App.tsx
```

### 题目库

页面文件：

```text
frontend/src/pages/QuestionListPage.tsx
```

已支持：

```text
查看题目列表
新增题目入口
导出入口
标签搜索
多标签筛选
已选标签展示
清空筛选
题目卡片展示
答案隐藏 / 显示
查看详情
编辑题目
删除题目确认
```

题目库顶部已有统计卡片：

```text
未掌握
复习中
已掌握
待标记
```

### 复习面板

页面文件：

```text
frontend/src/pages/ReviewDashboardPage.tsx
```

已支持：

```text
展示掌握状态统计
优先展示未掌握、复习中、待标记题目
提示推荐使用的状态标签
```

当前掌握状态不是数据库字段，而是根据标签名自动推导。

推导逻辑文件：

```text
frontend/src/utils/questionStatus.ts
```

识别规则：

```text
包含 未掌握 或 错题    => 未掌握
包含 复习中 或 待复习  => 复习中
包含 已掌握 或 掌握    => 已掌握
其他                  => 待标记
```

### 标签配置

页面文件：

```text
frontend/src/pages/TagSettingsPage.tsx
```

已支持：

```text
创建标签
选择标签颜色
搜索标签
查看标签使用题数
删除标签
显示标签总数和题目总数
```

这个页面是为了后期大量标签管理专门拆出来的。

### 导出中心

页面文件：

```text
frontend/src/pages/ExportCenterPage.tsx
```

已支持：

```text
查看当前将导出的题目数量
按标签筛选导出范围
导出 Markdown 文件
```

### 新增、编辑、详情页

页面文件：

```text
frontend/src/pages/QuestionCreatePage.tsx
frontend/src/pages/QuestionEditPage.tsx
frontend/src/pages/QuestionDetailPage.tsx
```

已支持：

```text
新增题目
编辑题目
查看题目详情
详情页隐藏 / 显示答案
详情页删除题目
```

## 6. 当前数据库模型

### questions 表

字段：

```text
id
question
answer
note
source
created_at
updated_at
```

说明：

```text
前端和 API 不再要求用户填写“标题”字段。
当前数据库模型里仍保留隐藏的 title 列，只用于兼容旧 SQLite 表；新增/编辑题目时后端会自动用问题首行生成内部标题。
用户实际录入和展示只围绕 question / answer / note / source / tags。
```

### tags 表

字段：

```text
id
name
color
created_at
```

### question_tags 表

字段：

```text
question_id
tag_id
```

说明：

```text
一个题目可以绑定多个标签
一个标签可以绑定多个题目
删除题目或标签时，关联关系会同步删除
```

## 7. 当前启动方式

后端：

```powershell
cd backend
uv run uvicorn app.main:app --reload --port 8000
```

前端：

```powershell
cd frontend
npm install
npm run dev
```

访问地址：

```text
前端：http://localhost:5173
后端文档：http://localhost:8000/docs
```

当前主要页面：

```text
复习面板：http://localhost:5173/review
题目库：http://localhost:5173
新增题目：http://localhost:5173/questions/new
标签配置：http://localhost:5173/settings/tags
导出中心：http://localhost:5173/export
```

## 8. 已验证情况

已经执行过：

```powershell
cd frontend
npm run build
```

结果：

```text
构建成功
```

后端也做过接口自测，确认以下流程可用：

```text
创建标签
创建题目
多标签筛选
Markdown 导出
```

## 9. 当前设计取舍

### 掌握状态暂时不进数据库

当前“未掌握 / 复习中 / 已掌握 / 待标记”是前端根据标签推导出来的。

优点：

```text
不用改数据库
可以马上用
状态本质也是一种标签，灵活
```

后续如果要更正式，可以在 questions 表增加字段：

```text
status
```

可选值：

```text
unmastered
reviewing
mastered
```

### 标签编辑暂未实现

当前标签支持创建、查询、删除，但不支持重命名和改颜色。

后续可以补：

```text
PUT /api/tags/{id}
```

前端标签配置页可以加“编辑标签”弹窗。

## 10. 建议下一步开发

优先级从高到低：

1. 增加题目搜索

```text
按问题、答案、笔记搜索
```

2. 增加标签编辑

```text
修改标签名
修改标签颜色
```

3. 增加正式掌握状态字段

```text
questions.status
```

这样可以不用依赖标签名推导。

4. 增加题目排序

```text
按更新时间
按创建时间
按掌握状态
按标签数量
```

5. 增加复习记录

可以新增表：

```text
review_logs
```

记录：

```text
question_id
reviewed_at
result
```

6. 接入 AI 能力

可以做：

```text
AI 自动生成标签
AI 总结答案
AI 生成面试表达版答案
AI 根据错题生成复习计划
相似题检索
```

## 11. 简历描述当前版本

可以这样写：

```text
Offer 题库助手是一个基于 React + TypeScript + FastAPI + SQLite 的个人求职题库工作台，支持题目录入、编辑、删除、答案隐藏、多标签管理、多标签组合筛选、复习状态统计和 Markdown 导出。项目采用前后端分离架构，后端提供 RESTful API，前端以工作台形式拆分题目库、复习面板、标签配置和导出中心，并为后续接入 AI 自动打标签、答案总结和智能复习计划预留扩展空间。
```
